<h1><%= groups %></h1>
<div class="row">
    <div class="col-sm-2">
      <p class="mt-4">User name : <%= user.name %></p>
    </div>
    <div class="col-sm-8">
      <% include product.ejs %>
    </div>
    <div class="col-sm-2">
        <p class="mt-4">
            <p>User list</p>
            <% for(var i=0; i < users.length; i++) { %>
                <p><%= users[i].name %></p>
            <% } %>
        </p>
        <a href="/homepage">HomePage</a>
        <a href="/group_cart">Group Cart</a>
    </div>
</div>
  
  
  